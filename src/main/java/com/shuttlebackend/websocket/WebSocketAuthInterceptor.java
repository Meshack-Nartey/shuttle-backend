package com.shuttlebackend.websocket;

// Handshake interceptor removed; keep as a no-op class to avoid bean loading issues during migration.
public class WebSocketAuthInterceptor {
    // deprecated - STOMP channel interceptor in StompAuthChannelInterceptor replaces this functionality
}
