spring:
  application:
    name: store

  datasource:
    url: jdbc:mysql://localhost:3306/shuttle-backend?createDatabaseIfNotExist=true
    username: root
    password: 1Science,3
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
