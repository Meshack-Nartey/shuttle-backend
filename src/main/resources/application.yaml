spring:
  application:
    name: store

  datasource:
    url: jdbc:mysql://localhost:3306/shuttle_backend_new?createDatabaseIfNotExist=true
    username: root
    password: 1Science,3
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    open-in-view: false

  sql:
    init:
      mode: never   # STOP Spring from running schema.sql / data.sql

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  thymeleaf:
    check-template-location: false

logging:
  level:
    org.hibernate.type.descriptor.sql: TRACE
    org.hibernate.orm.jdbc.bind: TRACE

jwt:
  secret: "A92F3C19A8E6D1F7B92744AEE1C5A3C0D998DAB12399F567B8C9D01A44EE9A34"
  expiration: